# Upstox WebSocket client config
# Copy to server/.env and fill values

# Bearer access token from Upstox OAuth flow (paste the token string only)
UPSTOX_ACCESS_TOKEN=

# Market Data Feed Authorize endpoint (verify against current docs)
UPSTOX_AUTHORIZE_URL=https://api.upstox.com/v3/market-data/authorize

# Comma-separated list of instrument keys to subscribe
# Example: NSE_EQ|INE669E01016,NSE_INDEX|NIFTY_50
UPSTOX_INSTRUMENT_KEYS=

# Subscription mode - verify the exact accepted values/enums in the proto
# Common modes: ltp, full
UPSTOX_MODE=ltp

# Path to the Upstox market data .proto schema
# Place your .proto under server/schemas/ and set the filename below
UPSTOX_PROTO_PATH=./schemas/upstox_marketdata.proto
